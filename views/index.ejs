<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>URL Shorter</title>
  </head>
  <body class="bg-gradient-to-bl from-sky-50 to-green-100 bg-no-repeat h-svh">
    <div class="container mx-auto max-w-4xl py-8 px-4">
      <h1 class="text-3xl font-bold mb-6">URL Shrinker</h1>
      <form action="/shortUrls" method="POST" class="mb-8">
        <div class="flex flex-col space-y-4">
          <div>
            <label for="fullUrl" class="block text-sm font-medium text-gray-700"
              >Full URL</label
            >
            <input
              type="text"
              name="fullUrl"
              id="fullUrl"
              placeholder="Enter the complete URL"
              required
              class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm sm:leading-9"
            />
          </div>
          <div>
            <label
              for="shortUrl"
              class="block text-sm font-medium text-gray-700"
              >Short URL Name</label
            >
            <input
              type="text"
              name="shortUrl"
              id="shortUrl"
              placeholder="Name"
              required
              class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm sm:leading-9"
            />
          </div>
          <button
            type="submit"
            class="inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
          >
            Shorten URL
          </button>
        </div>
      </form>
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th
              scope="col"
              class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-6"
            >
              Full URL
            </th>
            <th
              scope="col"
              class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"
            >
              Short URL
            </th>
            <th
              scope="col"
              class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900"
            >
              Clicks
            </th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200 bg-white">
          <% shortUrls.forEach(shortUrl => { %>
          <tr>
            <td
              class="whitespace-nowrap py-4 pl-4 pr-3 text-sm font-medium text-gray-900 sm:pl-6"
            >
              <a
                href="<%= shortUrl.full %>"
                class="text-indigo-600 hover:text-indigo-900"
                ><%= shortUrl.full %></a
              >
            </td>
            <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
              <a
                href="<%= shortUrl.short %>"
                class="text-indigo-600 hover:text-indigo-900"
                ><%= shortUrl.short %></a
              >
            </td>
            <td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
              <%= shortUrl.clicks %>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </body>
</html>
